#include "../../include/layers/image_layer.h"


namespace BigBang {

template<typename dtype>
void ImageLayer<dtype>::Forward_CPU(const Tensor<dtype>* bottom, Tensor<dtype>* top) {
	//const int offset 
}

template<typename dtype>
void ImageLayer<dtype>::Forward_GPU(const Tensor<dtype>* bottom, Tensor<dtype>* top) {

}


}